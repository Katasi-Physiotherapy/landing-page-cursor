---
const painPoints = [
  'Persistent back or neck pain',
  'Shoulder or knee injuries',
  'Sports injuries that won\'t heal',
  'Stiffness from long hours at a desk',
  'Pain that keeps coming back',
];
---

<section class="pain-points">
  <div class="container">
    <h2 class="section-title">Are you dealing with any of these?</h2>
    <ul class="pain-points__list">
      {painPoints.map((point) => (
        <li class="pain-points__item">{point}</li>
      ))}
    </ul>
    <p class="pain-points__message">
      You don't have to live with pain. With the right treatment, your body can heal and become stronger than before.
    </p>
  </div>
</section>

<style>
  .pain-points {
    padding: var(--space-3xl) 0;
    background: var(--color-surface);
  }
  
  .section-title {
    font-size: clamp(1.5rem, 3vw, 2rem);
    text-align: center;
    margin: 0 0 var(--space-2xl);
    color: var(--color-text);
  }
  
  .pain-points__list {
    list-style: none;
    margin: 0 auto var(--space-xl);
    padding: 0;
    max-width: 560px;
  }
  
  .pain-points__item {
    padding: var(--space-md) var(--space-lg);
    margin-bottom: var(--space-sm);
    background: var(--color-background-alt);
    border-radius: var(--radius-md);
    border-left: 4px solid var(--color-primary);
    font-weight: 500;
  }
  
  .pain-points__message {
    text-align: center;
    font-size: 1.125rem;
    color: var(--color-text-muted);
    max-width: 560px;
    margin: 0 auto;
    line-height: 1.7;
  }
</style>
